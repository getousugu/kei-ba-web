# 中央競馬場 最低限仕様

## 開催方式

- 中央競馬場ではレースを常時開催する。
- 発走はサーバーが管理する決まった実時間に行う。
- 馬券受付の締切とレース進行は、端末時計ではなくサーバー時刻を参照する。
- レース開催設定は基本的におまかせとし、盛り上がり演出は有効にする。

## 出走馬

- 中央サーバーの馬プールは初期800頭、最大1,000頭を目安とする。
- 各レースの出走馬は、発走前の所定時刻に馬プールから選出する。
- 選出時点で参加中のプレイヤーに名付け馬がいる場合、その馬を低確率で候補へ加える。
- 名付け馬の選出率、連続出走制限、能力帯の調整は仮機能として後から決定する。

## 参加導線

1. ホーム画面で「中央競馬場へ」を押す。
2. 通常ルームの作成・参加と開催設定を省略する。
3. 初期実装では馬券受付画面へ直接移動する。
4. サーバー時刻の締切後に発走する。

パドックは構想を保留し、実装する場合だけ中央競馬場と馬券受付の間へ追加する。

途中参加者は、受付中なら当該レース、締切後なら次のレースへ参加する。

## WIN5と人数表示

- 中央競馬場のWIN5は常時開催扱いとする。
- 画面上部に中央競馬場の現在参加者数を常時表示する。
- 同じ場所にWIN5参加人数も常時表示する。
- WIN5の対象レース切替、参加、脱落、配当、キャリーオーバーはサーバーを正とする。

## ホーム画面

- ルームコード入力欄の直下に「クイック参加」を置く。
- サーバー実装前のクイック参加は灰色の準備中表示にする。
- その下に通常ルームとは見た目を分けた「中央競馬場へ」を置く。

## サーバー側で保持する最低限の状態

- 現在と次回のレースID、発走時刻、馬券締切時刻
- 大規模な中央馬プールと今回の出走馬
- 接続中の参加者とWIN5参加者
- 馬券、残高、配当、WIN5進行
- 確定したレース条件、結果、サーバー時刻

## 実装段階

- 第1段階ではサーバー時刻、開催予定、馬プール、出走馬、参加人数、WIN5人数を中央管理する。
- 第2段階で既存のレース式をサーバーへ移し、結果を中央で一度だけ確定する。
- 第3段階で馬券、残高、配当、WIN5決済、名付け馬登録を中央管理する。
- 公開前のローカルサーバーはCloudflareアカウントや認証情報を必要としない。

## 中央獲得賞金とグレード

- 馬には通算成績とは別に「中央獲得賞金」を持たせる。
- 中央獲得賞金は馬の経歴を示す値で、プレイヤーは使用できない。
- 通常ルームの結果では加算せず、中央競馬場の確定結果だけでサーバーが加算する。
- GⅠは1時間に1回、毎時00分発走を基本とする。
- 毎時30分をGⅡ、15分と45分をGⅢ、その他を一般競走とする案を初期値にする。
- GⅡ・GⅢ・一般競走は中央獲得賞金0の馬も出走できる。
- GⅠは中央獲得賞金が1以上の馬だけを出走候補にする。
- GⅡ以下では賞金上位馬の抽選重みを最大5%だけ高くする。能力値そのものには補正を加えない。
- グレードや賞金は出走選定にだけ関係し、レース式を直接有利にしない。
